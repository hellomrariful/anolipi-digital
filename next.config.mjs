/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'i.postimg.cc',
        'flaticon.com',
        'cdn-icons-png.flaticon.com'
      ],
    },
  };
  
  export default nextConfig;  